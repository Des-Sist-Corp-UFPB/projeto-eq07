spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/corridas_dev
    username: corridasdb
    password: C@rR!d@db
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    # Localização dos scripts de migração SQL
    locations: classpath:db/migration

api:
  security:
    token:
      secret: "teste_secret"

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always

logging:
  level:
    br.ufpb.dsc.mercado: DEBUG
    org.springframework.web: DEBUG
