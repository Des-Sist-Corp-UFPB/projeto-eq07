server:
  port: 8080
spring:
  datasource:
    # Se DATABASE_URL for injetada pelo Docker, o Spring usa. Senão, adota o padrão local 'corridas_dev'
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/eq07}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration

api:
  security:
    token:
      # Permite injetar um secret forte em produção. Se não houver, usa o padrão.
      secret: ${API_SECURITY_TOKEN_SECRET:teste_secret}

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always

logging:
  level:
    # Corrigido de 'mercado' para o pacote real do seu projeto 'corrida'
    br.ufpb.dsc.corrida: DEBUG
    org.springframework.web: DEBUG